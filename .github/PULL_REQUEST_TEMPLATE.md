## What this changes

<!-- And why. If it fixes an issue, link it. -->

## How it was tested

<!-- Which of these applies. Say which reader and display if you used hardware:
     those are the usual source of behaviour that does not reproduce. -->

- [ ] Built locally (`cmake -B build && cmake --build build`)
- [ ] Exercised the interface through the FIFO with no reader attached
- [ ] Tested on a unit with a real reader and a real card
- [ ] Documentation only, nothing to test

## Checklist

- [ ] No card number, key, token or production endpoint added to the repository
- [ ] Documentation under `docs/` updated if behaviour or deployment changed
- [ ] `CHANGELOG.md` updated for a user-visible change
